spring:
#  메모리DB를 사용하는 url이다. h2 db가 켜져있지 않아도 테스트는 따로 동작한다.
#  또한 스프링 부트에서는 아래 설정들이 설정되어 있지않으면 메모리db를 사용한다.
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create #운영에서 사용 x
#    properties:
#      hibernate:
#        show_sql: true #운영에서는 사용 x
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
#   org.hibernate.type: trace